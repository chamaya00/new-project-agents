/**
 * Placeholder so the four CI scripts have something real to check.
 * Replace this with the first actual feature - the gate does not care
 * what the code is, only that typecheck, lint, test, and build all pass.
 */
export function greet(name: string): string {
  return `Hello, ${name}!`;
}
